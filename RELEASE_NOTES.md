## What's New

- npm README에서 로고 및 아키텍처 이미지가 표시되지 않던 문제 수정
- docs 사이트 SPA 라우팅 복원 — 직접 URL 접근 시 404 발생하던 문제 수정
- README 문서 링크 수정 (`merge-lifecycle` → `merge-generation`, 존재하지 않는 `agent-integration` 제거)
- 로컬 빌드 시 `+dev.{hash}` 버전 suffix 추가
- `cleanupLegacyProjectSkills`에서 reapdev 스킬이 삭제되던 문제 수정
- docs workflow에 `workflow_dispatch` 트리거 추가
