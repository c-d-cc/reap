---
description: "REAP Update — Upgrade project structure and check for updates"
---

Run `reap init --migrate` and follow the stdout instructions exactly. The migration runs in multiple phases — execute each phase command as directed by the output.
