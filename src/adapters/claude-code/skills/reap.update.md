---
description: "REAP Update — Upgrade project structure and check for updates"
---

Run `reap update` and follow the stdout instructions exactly.

- For v0.15 projects: automatically delegates to migration workflow (multi-phase)
- For v0.16 projects: syncs project structure (config backfill, missing directories, CLAUDE.md repair)
