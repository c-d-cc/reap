---
description: "REAP Update — Run migration or check for updates"
---

Run `reap init --migrate $ARGUMENTS` and follow the stdout instructions exactly.

If the project is already v0.16, inform the user that update agent is not yet available in v0.16.
