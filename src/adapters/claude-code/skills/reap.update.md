---
description: "REAP Update — Upgrade project structure and check for updates"
---

Run `reap init --migrate $ARGUMENTS` and follow the stdout instructions exactly. The migration runs in multiple phases — execute each phase command as directed by the output.

If the project is already v0.16, inform the user that the full update agent is not yet available in v0.16.
