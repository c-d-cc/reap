---
description: "npm Install — npm에서 최신 배포 버전 설치"
---

# npm Install

npm registry에서 최신 배포 버전을 설치합니다. +dev 로컬 빌드를 정식 버전으로 교체할 때 사용.

## Steps

1. `npm install -g @c-d-cc/reap@latest`
2. `reap --version` 으로 설치 확인
