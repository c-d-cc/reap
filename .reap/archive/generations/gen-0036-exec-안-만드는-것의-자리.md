---
id: gen-0036-exec
slug: 안-만드는-것의-자리
type: exec
backlog: bk-75bafa
title: 만들지 않기로 한 것의 자리 — carrier와 hooks
startedAt: 2026-08-23T05:10:07Z
startCommit: 503dd39
status: closed
closedAt: 2026-08-23T05:13:39Z
endCommit: d96e619
---

## Intent

`bk-75bafa`(carrier)를 소비한다. **`bk-c4fd73`(hooks)도 함께 소비한다** — 둘이 **같은 판단**을 요구하기 때문이다: *만들지 않기로 한 것을 spec에서 어떻게 다루는가.* 따로 정하면 답이 어긋난다.

**무엇이 되면 끝인가:** spec에 "있는데 갈 곳이 없는" 것이 남지 않는다. 둘 다 어떻게 다뤄지는지가 문서에 적힌다.

## Working Plan — 구조가 답을 갈랐다

취향이 아니라 **의존 관계**가 둘을 다르게 만든다.

- **hooks는 구조에 이미 자리가 있다.** `src/store.ts`의 `DIRS`가 `hooks`·`hooks/conditions`를 만들고 `03-storage.md:51`의 레이아웃에도 있다. **`init`을 부르면 그 디렉토리가 생긴다.** 즉 지금도 프로젝트마다 빈 자리가 놓이고 있다 → **spec에 남기되, 언제 채우는지를 적는다**
- **carrier는 아무것도 의존하지 않는다.** 정의(`05-knowledge.md`)와 명령·`doctor` 항목(`04-commands.md`)뿐이고 코드에도 레이아웃에도 흔적이 없다 → **spec에서 빼고 `idea/research/`로 옮긴다.** `05-knowledge.md`가 그 자리를 *"결론은 없지만 다시 찾지 않게 해주는 것"*으로 규정했고 carrier가 정확히 그것이다

## Notes

**`--backlog`에 하나만 줄 수 있어 `bk-75bafa`를 줬다.** `bk-c4fd73`은 여기 Intent에 적는다 — `evolve`가 그렇게 하라고 말한다.

다만 `evolve`의 병합 규칙이 *"같은 명령이나 같은 파일을 요구하면"*이라고 적혀 있는데, **이 둘은 파일이 다르고 판단이 같다.** 규칙의 기준이 파일이 아니라 판단이어야 하는 것 아닌가 — 이 세대가 그 첫 반례다.

## Outcome

**spec에 "있는데 갈 곳이 없는" 것이 없다.** 둘을 다르게 다뤘고, 가른 것은 취향이 아니라 **구조**다.

| | 어떻게 | 왜 |
|---|---|---|
| **hooks** | spec에 남기고 `07-orchestrate.md`에 **"언제 만드는가 — 아직 아니다"** 절을 더했다. 신호 셋과 그 첫 시험 결과까지 | `.reap/hooks/`·`hooks/conditions/`가 `init`이 만드는 디렉토리로 **이미 구조에 있다.** 지금도 프로젝트마다 빈 자리가 놓인다 |
| **carrier** | spec에서 뺐다. `idea/research/`의 `idea-a608f2`로 옮기고 졸업 조건을 적었다 | 정의와 명령·`doctor` 항목뿐이고 **코드에도 레이아웃에도 의존하는 것이 없었다** |

**신호를 backlog에 두지 않고 spec으로 올린 것이 요점이다.** backlog는 소비되면 archive로 내려가므로 거기 있는 신호는 안 읽힌다. hooks 규약을 소유한 문서가 그 자리다.

`carrier`를 빼면서 잔여 하나를 더 고쳤다 — `06-agent.md:182`가 *"같은 규율을 네 곳에 복사하는 순간 그것은 carrier 문제가 된다"*로 개념을 설명 용어로 쓰고 있었다. 개념이 사라졌으므로 문장이 자립하게 고쳤다.

**`idea/`의 첫 사용이다.** 36세대 동안 0개였고, `ms-006`의 fitness가 *"`idea/`를 쓰게 됐는가"*를 묻는다.

## 남긴 것

**`bk-97630a` — 세대를 열기 전에 세대로 남길 값이 있는지 먼저 판단한다.** 사람이 `gen-0034-fix`를 짚었다: 주석 두 블록 지우고 spec 한 문단 더한 일에 세대를 열었고, **커밋이면 충분했다.** `evolve`가 *어느 축인지*는 길게 다루면서 *열 값이 있는지*는 다루지 않는 것이 원인이다.

## `evolve`의 병합 규칙에 첫 반례

`evolve`는 *"두 항목이 같은 명령이나 같은 파일을 요구하면 함께 소비한다"*고 적는데, **이 둘은 파일이 다르고 판단이 같았다.** 따로 정했으면 답이 어긋났을 것이다 — 실제로 처음엔 carrier도 hooks처럼 spec에 남기려 했다가 의존 관계를 보고 갈랐다.

**기준이 파일이 아니라 판단이어야 한다.** 다만 표본 하나로 규칙을 고치지는 않았다.

## 검증

- `bun test` 114 · `hook.test.sh` 통과 · spec 링크 깨진 것 0
- `carrier` 잔여는 로드맵의 **왜 뺐는지 설명**뿐이다
