# 2 — selfview 재이주와 대조

`~/cdws/selfview` 브랜치 `reap-v018-migration`을 `dev`로 리셋(`git checkout dev && git branch -D reap-v018-migration && git checkout -b reap-v018-migration`)한 뒤 개정된 SKILL 8단계를 처음부터. 끝에 사람에게 보여줄 것: 상태 줄 전문, milestone.md·tasks 목록, backlog 목록, plan 등록 목록, genome diff 요약, CLAUDE.md REAP 절, verify-migration.sh 출력. 사람이 "지워도 된다"고 하면 task 끝. 아니면 지적을 tasks/1로 되돌린다.
