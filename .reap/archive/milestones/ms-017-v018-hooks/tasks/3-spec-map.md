# 3 — spec과 map

- `docs/superpowers/specs/reap/07-orchestrate.md`의 "언제 만드는가 — 아직 아니다" 절을 결정으로 바꾼다: 사람(2026-09-01)이 제공을 결정했고, 신호 셋은 **이벤트를 더할 때**의 기준으로 남긴다
- 04-commands의 `make hook` 줄이 실제 옵션과 같은지 대조
- `src/templates/map.md`의 `hooks/` 줄과 이 리포의 `.reap/map.md`(씨앗이라 손으로) 갱신
- 03-hooks.md의 "검증할 동작"이 전부 테스트에 있는지 표로 대조하고 없는 것은 만든다

한 세대. task 2와 같은 세대에서 해도 된다.
