# handoff — ms-007

**7.1~7.4 전부 됐다** (`gen-0050` interview · `gen-0051` init). 남은 것은 새 세션 확인과 사람의 닫기.

- `interview`·`init` skill이 있고 `evolve`·`carve-milestone`·`loop`·`init`이 `interview`를 가리킨다
- `init --check`가 씨앗 판정을 한다
- **cold start 왕복은 검증하지 않았다** — 사람이 실사용으로 확인하고 고칠 것을 돌려준다. fitness 여섯은 전부 그 뒤다

첫 실사용 후보: 새 폴더 하나, `CLAUDE.md`가 있는 기존 리포 하나.

cleanup: `gen-0046`(init 설계 — 내용이 `06-agent.md` init 절에 반영됨)·`gen-0050`·`gen-0051`(skill이 그 결과다)을 archive로.
