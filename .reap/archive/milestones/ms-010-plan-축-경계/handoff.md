# handoff — ms-010 종료

**전제가 틀린 milestone이었다.** track은 `gen-0044-plan`이 전부 걷어냈다. fitness는 `milestone.md` 맨 아래.

## 남긴 것

- **`plan/` 최상위** (10.1) — 사람이 유지하라고 했다
- **`mark milestone --focus`가 초점을 옮긴다** — 더하기만 하던 결함을 `gen-0041`이 고쳤다
- **`vision/memory/tracks.md` 해체** — 다섯이 각자 자리로 갔고 그 배치는 track과 무관하게 유효하다

## archive로 내린 세대

넷 다 `archive/generations/`로 내렸다 — `gen-0040-plan`(track 설계) · `gen-0041-exec`(`plan/` 최상위) · `gen-0042-exec`(track 구현) · `gen-0043-exec`(이주).

**지우지는 않았다.** `gen-0040`은 track이 왜 틀렸는지의 원본이고 `gen-0044`의 `Dead Ends`가 그것을 가리킨다. 참조는 경로가 아니라 id이므로 archive에 있어도 찾힌다.

**`gen-0044-plan`(철회)은 `life/`에 남긴다** — 다음에 이 빈칸을 채울 세대가 읽어야 한다.

## 다음이 볼 것

**`02-flow.md`의 `plan 축에는 아직 경계가 없다`**가 빈칸을 갖는다. 채우려면 *"plan을 만드는 과정"*이 무엇인지부터 — 묶음이 아니라는 것까지만 정해졌다.

다음 milestone은 `ms-005`(plan 축 완성)다. `author-plan`이 거기 있고, **그것이 이 빈칸과 맞닿는다.**
