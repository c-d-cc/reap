# 3 — README와 릴리스 노트

04-migrate-docs의 README 7절. 설치 명령은 ms-016의 실제 결과(`npm i -g @c-d-cc/reap@next`, 플러그인 `claude plugin marketplace add c-d-cc/plugins` → `claude plugin install reap@ctod-plugins`)와 06-release의 마켓플레이스 이름을 그대로. **Q5의 답이 없으면 마켓플레이스 항목 이름을 `reap`으로 가정하고 handoff에 남긴다.**

`RELEASE_NOTES.md` 0.18.0 절 — 첫 `## ` 블록. v0.17 노트의 톤(사용자에게 의미 있는 것만, 9f13220)을 따른다.

upgrade agent(`~/cdws/reap_v17/docs/upgrade-agent/reap-upgrade.md`) 3단계 문구와 README 설치 절이 같은 명령을 말하는지 대조 — 다르면 agent 쪽을 고친다(그 리포는 v0.17 브랜치; 커밋은 ms-019가 0.17.8 준비와 함께).
