# handoff — ms-020

## 지금 어디까지 왔는가

task 1(`tasks/1-skill-and-brief.md`) 끝. `evolve/SKILL.md`의 위임 판단 절과
`references/delegate-brief.md`, `complete/SKILL.md`의 위임 세대 검토 절,
`06-agent.md`의 evolve 셋째 판단, `record-vocabulary.md`의 `Delegation` 항목
전부 gen-0084(worktree `reap-wt-delegate`, 브랜치 `ms-020-delegate`)에서 됐다.
`bun test`·doctor 변화 없음(산문만).

## 다음에 무엇부터

**실물은 아직이다.** 이 milestone의 Exit Criteria 마지막 항목 — 작은 exec
세대 하나를 실제로 위임 모드로 열어 subagent가 수행하고 주 세션이 닫는
것 — 이 남았다. **ms-021의 첫 세대가 그 실물이 된다.** ms-021을 열 때
`delegate-brief.md`를 실제로 채워 써보고, 그 결과로 brief 템플릿의 구멍이
드러나면 이 milestone으로 돌아와 템플릿을 고친다.

gen-0084는 아직 열려 있다 — merge 전에 주 세션이 diff·테스트·Outcome을
검토하고 `complete`로 닫는다.

## 걸려 있는 것

없음.
