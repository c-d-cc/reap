# ps-5e948f — reap v0.18 완성·출시 계획

root: `./docs/reap-plan/reap_v_0_18_release`

## 성격

ps-4b485d와 같은 **작전 계획**이다 — 무엇이 참이어야 하는가에 더해 순서(발행 순서)와 판정(v0.17 대조)을 담는다. 범위는 발행 직전까지. 발행은 사람.

## 어떻게 읽는가

`README.md`가 색인. `01-gap.md`의 "만든다" 표(G1~G8)가 milestone 후보의 전부다. `05-open.md`는 사람의 답을 기다리는 방향 질문이며 **실행 근거가 아니다** — 답이 오면 그 절이 결정으로 바뀌고 01에 행이 더해진 뒤에야 자른다.

## 어떻게 쓰는가

- 결정과 미결을 가른다. 사람의 답은 출처(날짜)와 함께
- 실측 주장에는 경로·명령을 적는다. 실행해보지 않은 코드를 넣지 않는다
- spec(ps-4f2a91)과 겹치는 규범은 여기 옮겨 적지 않고 링크한다. 이 세트가 spec을 바꾸는 곳(hooks "언제 만드는가")은 spec 파일 자체를 고친다
- v0.17에 없는 기능 요구가 생기면 01의 표에 행을 더한 뒤에 자른다

## 수명

0.18.0이 발행되면 소비 완료. 05의 미답 질문은 `idea/research/`로 옮긴다.
