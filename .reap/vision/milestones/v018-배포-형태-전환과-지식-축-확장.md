---
goal: 배포 형태를 사용자 도구로 인식되게 한다
status: open
main: true
createdAt: 2026-08-21T10:49:53.716Z
---

# v0.18 배포 형태 전환과 지식 축 확장

## Exit Criteria
- 사용자의 클라이언트에서 REAP 이 **하나의 도구로 보인다** — skill 19줄이 아니라 plugin 한 줄로,
  켜고 끄기·버전·출처가 함께
- **`reap install-skills` 가 사용자 홈 디렉토리에 파일을 흩뿌리지 않는다.** adapter 의 사용자 레벨
  설치 코드와 그것을 지키던 게이트 절이 사라진다
- `.reap/plan/` · `.reap/idea/` 가 존재하고, 무엇이 어느 자리에 가는지 genome 이 답한다
- memory 가 flat + 메타데이터로 동작하고, prompt 에는 **살아있는 것의 요약만** 올라간다
- `/reap.interview` 와 `/reap.plan` 이 실제로 호출된다
- 0.18 이 발행되고, `migration/v0.18.0.md` 가 기존 프로젝트에 규칙 변경을 전달한다

## Out of Scope
- **Cell (다중 pipeline 상호작용)** — 공간축 단위이고 이 milestone 은 시간축이다. 섞지 않는다
- **Evaluator 의 Vision/Goal 위임** — milestone 이 생기면서 성격이 바뀌었다. 다시 볼 것이지 지금 할 것이 아니다
- **indexer 기능**(community detection / process tracing) — 배포 형태와 무관하다
- **`backlogs_v0.17_residual/` 9건** — plugin 전환 후 무엇이 살아남는지 판정하고 나서
- **Embryo → Normal 전환** — 사용자 명시 검토 사항

## Generations
- [x] gen-097 — milestone 도입 (main 에 있고, plugin 브랜치가 상속한다)
- [ ] plugin 배포 형태 리서치·설계 — A/B 확정. `vision/design/plugin-distribution.md` 의 미독 절 3개를 먼저 읽는다
- [ ] plugin 전환 구현 (별도 브랜치)
- [ ] 지식 축 경계 통합 설계 — milestone · plan · idea · memory 4축을 한 세대에서 닫는다
- [ ] memory 를 flat + 메타데이터로 재설계
- [ ] `.reap/plan/` · `.reap/idea/` 자리 신설
- [ ] interview skill
- [ ] `/reap.plan` skill
- [ ] 0.18 릴리즈 — bump + `migration/v0.18.0.md` + docs 5 로케일 (셋은 분리 불가)
