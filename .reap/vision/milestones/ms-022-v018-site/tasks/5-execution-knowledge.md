# 5 — Execution 축 4 + 지식 4

| 라우트 | 쪽 | 담을 것 | v0.17 골격 참고 |
|---|---|---|---|
| /docs/generation | Generation | exec·fix 두 유형과 근거(milestone·backlog), 세션 바인딩(.session, 하나만), 열 값이 있는가, 기록 어휘(Intent·Working Plan·Dead Ends·Outcome), 커밋 규칙, `make generation`·`mark generation`·`bind` | LifecyclePage |
| /docs/delegation | 위임 모드 | 직접/위임 판단 신호 셋, brief 템플릿(evolve/references/delegate-brief.md의 항목), subagent가 지키는 것(make·mark 금지 등), 주 세션의 검토와 complete, 병렬이면 orchestrate | (신설) SelfEvolvingPage의 절 구성 |
| /docs/backlog | Backlog | 항목 만들기(`make backlog --type`), exec의 근거로 쓰기(`--backlog`), 소비(`mark backlog --consumed`), life에 남는 이유와 archive, 두 항목이 겹칠 때 | BacklogPage |
| /docs/closing-milestone | Milestone 닫기와 Fitness | 종료 조건 판정은 사람, 자를 때 써둔 fitness 질문, 순서(fitness → cleanup → mark --closed), cleanup의 기준(참고 가치), handoff에 남길 것/lessons로 올릴 것 | LifecyclePage completion 절 |
| /docs/genome | Genome | application·evolution·invariants, 매 세션 주입, 씨앗과 `init --check`, init skill이 채우는 순서, 크기 안내선 | GenomePage |
| /docs/environment | Environment | summary(주입)·source-map(선택)·resources(필요할 때), 무엇을 쓰고 무엇을 안 쓰는가 | EnvironmentPage |
| /docs/vision-memory | Vision과 Memory | vision/의 milestones와 memory/lessons.md, 나가는 문(졸업), handoff와 lessons의 구분, memory를 주입하지 않는 이유는 쓰지 않고 "주입되지 않는다"만 | VisionPage·LineagePage |
| /docs/code-intelligence | Code Intelligence | 있는 내용을 새 목차·톤에 맞춤. index 명령, 15개 언어, 커밋 단위, index vs grep, 해석률 | CodeIntelligencePage |

규칙은 task 4와 같다. 실물 예시는 임시 리포에서 실제로 찍는다.
