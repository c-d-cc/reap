# 2 — 이주 왕복

`~/cdws/reap_v17/.reap`을 담은 표본 리포(scratchpad, git init + commit). upgrade agent 본문(`docs/upgrade-agent/reap-upgrade.md`) 1~5단계를 **그대로** 따르되 2단계의 npm 설치만 tarball로. 4단계 `/reap:migrate`는 skill 8단계 전부 — subagent 이주 포함. 끝은 doctor 0 · 기록 파일 · `.reap-v0_17/` 무손상 · 홈 정리 목록이 제시됐고 수행하지 않음(사람 환경).

agent 본문과 skill·README가 어긋난 지점은 전부 backlog가 아니라 **이 세대에서 고친다** — 어긋남이 곧 결함이다.
