# Objective

## Goal
Backlog 3건 수정 — frontmatter hard-gate, status/help 버전 출력 (+ latest 확인), completion auto-archive

## Completion Criteria
- current.yml + artifact에 AI 직접 수정 금지 frontmatter 추가
- reap status / reap run help 에 버전 + latest 여부 출력
- completion의 consume/archive가 genome phase 이후 자동 실행
- 537 tests 유지 + 신규
