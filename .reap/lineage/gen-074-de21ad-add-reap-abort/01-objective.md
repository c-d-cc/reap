# Objective

## Goal
`/reap.abort` 슬래시 커맨드 추가. 진행 중인 generation을 중단하고 초기 상태로 복귀.
코드 처리(rollback/stash/hold), backlog 저장(abort 메타 포함) 유저 선택.

## Completion Criteria
- reap.abort.md 커맨드 템플릿 생성
- E2E 테스트 통과
- completion 단계에서 compression 동작 확인
