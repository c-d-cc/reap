{
  "name": "@c-d-cc/reap",
  "version": "0.16.0",
  "description": "Recursive Evolutionary Autonomous Pipeline — AI and humans evolve software across generations",
  "type": "module",
  "bin": {
    "reap": "dist/cli/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "dev": "bun src/cli/index.ts",
    "build": "bun build src/cli/index.ts --outdir dist/cli --target node && cp -r src/adapters/claude-code/skills dist/adapters/claude-code/ && cp -r src/templates dist/templates",
    "typecheck": "tsc --noEmit",
    "postinstall": "node dist/cli/index.js install-skills 2>/dev/null || true"
  },
  "dependencies": {
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
