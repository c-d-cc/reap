Create a VitePress documentation site for "REAP" (Recursive Evolutionary Autonomous Pipeline).

## Design Direction

- This is a **technical documentation site** (like VitePress, Docusaurus, or Read the Docs style) — NOT a marketing landing page or blog
- The primary purpose is to provide clear, navigable developer documentation with sidebar navigation, code blocks, and structured content
- Dark theme, technological, minimal — similar to VitePress dark mode
- Brand color: golden tone (#ba9a46 primary, #d4c68a light, #e1c699 lighter, #a88a3b dark)
- Background: #121212 base, #1a1a1a soft, #222222 mute
- Text: #f2f2f2 primary, #a0a0a0 secondary, #666666 muted
- Use the attached image as the logo/favicon (golden seed/leaf symbol)
- Clean VitePress default structure with custom theme colors
- Hero page should be full-width (no sidebar), content pages show sidebar with left navigation

## Site Structure

### Landing (Hero Page - index.md)
- Large logo + "REAP" title
- Badge: "Recursive Evolutionary Autonomous Pipeline"
- Headline: "Evolve software across generations"
- Tagline: "AI and humans collaborate through structured generations. The Genome evolves, the Civilization grows."
- Two CTA buttons: "Get Started" + "View on GitHub"
- Feature cards below hero showing 3-Layer Model summary (Genome → Evolution → Civilization)

### Sidebar Navigation

**Introduction**
- What is REAP?
- Why REAP?
- Quick Start

**Core Concepts**
- 3-Layer Model
- Genome
- Life Cycle
- Four-Axis Structure
- Backlog & Deferral

**Workflow**
- /reap.evolve
- Stage Commands
- Micro Loop
- Hooks

**CLI Reference**
- reap init
- reap status
- reap update
- reap fix

**Advanced**
- Lineage Compression
- Presets
- Entry Modes
- Comparison with Other Tools

## Page Content

### What is REAP?
REAP (Recursive Evolutionary Autonomous Pipeline) is a development pipeline where AI and humans collaborate to incrementally evolve an Application across successive Generations.

3-Layer Model diagram:
```
Genome (Design & Knowledge) → Evolution (Generational Progress) → Civilization (Source Code)
```

- **Genome** — Design and knowledge for building the Application. Architecture principles, business rules, conventions, and technical constraints. Stored in `.reap/genome/`.
- **Evolution** — The process by which Genome evolves and Civilization grows through repeated Generations.
- **Civilization** — Source Code. The entire project codebase outside `.reap/`.

### Why REAP?
Problems when developing with AI agents:

| Problem | REAP Solution |
|---------|--------------|
| **Context Loss** — Agent forgets project context every new session | **SessionStart Hook** — Every session injects full Genome + generation state automatically |
| **Scattered Development** — Code modified with no clear goal | **Generation Model** — Each generation focuses on one objective with a structured lifecycle |
| **Design–Code Drift** — Documentation diverges from code | **Genome Mutation via Backlog** — Design defects logged during implementation, applied at Completion |
| **Forgotten Lessons** — Insights from past work are lost | **Lineage & Retrospectives** — Lessons accumulate in Genome, generations archived and compressed |

### Quick Start
```bash
# Install
bun install -g reap

# Initialize your project
cd my-project
reap init my-project

# Run a full generation in Claude Code
claude
> /reap.evolve "Implement user authentication"
```

`/reap.evolve` runs the entire generation lifecycle — from Objective through Completion — interactively. This is the primary command for day-to-day development.

Manual stage control:
```bash
> /reap.start            # Start a new generation
> /reap.objective        # Define objective + spec
> /reap.next             # Advance to next stage
> /reap.planning         # Create implementation plan
> ...
```

### Life Cycle
Each generation follows five stages:
```
Objective → Planning → Implementation ⟷ Validation → Completion
```

| Stage | What happens | Artifact |
|-------|-------------|----------|
| **Objective** | Define goal, requirements, and acceptance criteria | `01-objective.md` |
| **Planning** | Break down tasks, choose approach, map dependencies | `02-planning.md` |
| **Implementation** | Build with AI + human collaboration | `03-implementation.md` |
| **Validation** | Run tests, verify completion criteria | `04-validation.md` |
| **Completion** | Retrospective + apply Genome changes + archive | `05-completion.md` |

Create a visual flow diagram for this lifecycle using CSS (colored pill-shaped stages with arrows).

### Genome
The application's genetic information:
```
.reap/genome/
├── principles.md      # Architecture principles/decisions
├── domain/            # Business rules (per module)
├── conventions.md     # Development rules/conventions
└── constraints.md     # Technical constraints/choices
```

**Genome Immutability Principle**: The Genome is never modified directly during the current generation. Issues are recorded in the backlog and only applied at the Completion stage.

**Environment Immutability Principle**: The Environment is never modified directly during the current generation. External changes are recorded in the backlog and applied at the Completion stage.

### Four-Axis Structure
Display as a 2x2 card grid:

| Axis | Path | Role |
|------|------|------|
| **Genome** | `.reap/genome/` | Genetic information. Principles, rules, decisions |
| **Environment** | `.reap/environment/` | External context. API docs, infra, business constraints |
| **Life** | `.reap/life/` | Current generation's lifecycle. Progress state and artifacts |
| **Lineage** | `.reap/lineage/` | Archive of completed generations |

### Backlog & Deferral
All items stored in `.reap/life/backlog/` with markdown + frontmatter:
- `type: genome-change` — Applied to Genome at Completion
- `type: environment-change` — Applied to Environment at Completion
- `type: task` — Candidate goals for the next Objective

Partial completion is normal — tasks depending on Genome changes are marked `[deferred]` and handed to the next generation.

### /reap.evolve
The primary command. Runs the entire Generation lifecycle from start to finish, interactively with the human.

1. If no active Generation: runs `/reap.start` first
2. Reads `current.yml` to determine current stage
3. Executes the stage command (objective → planning → ... → completion)
4. Advances with `/reap.next` after each stage
5. Loops until generation completes

Handles validation failures (regress to implementation), pausing (state preserved), and stage skipping.

### Stage Commands
| Command | Description |
|---------|-------------|
| `/reap.start` | Start a new generation |
| `/reap.objective` | Define goal + requirements |
| `/reap.planning` | Task decomposition + implementation plan |
| `/reap.implementation` | Code implementation with AI + human |
| `/reap.validation` | Run tests, verify completion criteria |
| `/reap.completion` | Retrospective + apply Genome changes |
| `/reap.next` | Advance to the next stage |
| `/reap.back` | Return to a previous stage (micro loop) |

### Micro Loop
Any stage can regress to a previous stage. Artifact handling:
- Before target stage: Preserved
- Target stage: Overwritten (implementation only appends)
- After target stage: Preserved, overwritten upon re-entry

### Hooks
Projects can define hooks in `.reap/config.yml`:
```yaml
hooks:
  onGenerationStart:
    - command: "echo 'Generation started'"
  onStageTransition:
    - command: "echo 'Stage changed'"
  onGenerationComplete:
    - command: "reap update"
  onRegression:
    - command: "echo 'Regressed'"
```

### CLI Reference pages
- `reap init <name>` — Initialize project with options: `--mode` (greenfield/migration/adoption), `--preset` (e.g., bun-hono-react)
- `reap status` — Show current project and generation status
- `reap update` — Sync commands/templates/hooks to latest version. Option: `--dry-run`
- `reap fix` — Diagnose and repair `.reap/` directory structure

### Lineage Compression
| Level | Input | Output | Max lines | Trigger |
|-------|-------|--------|-----------|---------|
| **Level 1** | Generation folder (5 artifacts) | `gen-XXX.md` | 40 lines | lineage > 10,000 lines + 5+ generations |
| **Level 2** | 5 Level 1 files | `epoch-XXX.md` | 60 lines | 5+ Level 1 files |

### Presets
`reap init --preset bun-hono-react` — Bun + Hono + React stack

### Entry Modes
| Mode | Description |
|------|-------------|
| `greenfield` | Build a new project from scratch (default) |
| `migration` | Build anew while referencing an existing system |
| `adoption` | Apply REAP to an existing codebase |

### Comparison with Other Tools
(This is an internal documentation page, not prominently featured)

Traditional spec-driven tools (like Spec Kit) pioneered the idea of writing specifications before code. REAP builds on this concept but addresses key limitations:

**Static specs vs Living Genome**: Traditional tools treat specs as static documents that guide implementation. REAP's Genome is a living system — defects found during implementation feed back through the backlog and are applied at Completion. The design evolves with the code.

**No cross-session memory**: Most AI development tools lose context between sessions. REAP's SessionStart Hook injects full project context (Genome, generation state, workflow rules) into every new session automatically.

**Linear workflow vs Micro loops**: Traditional tools follow a linear flow (spec → plan → build). REAP supports structured regression — any stage can loop back to a previous one while preserving artifacts.

**Isolated tasks vs Generational evolution**: Each task in traditional tools is independent. In REAP, generations build on each other. Knowledge compounds through Lineage archives and Genome evolution.

**No lifecycle hooks**: REAP provides project-level hooks (onGenerationStart, onStageTransition, onGenerationComplete, onRegression) for automation.

## Evolution Flow (use as visual/diagram)
```
Generation #1 (Genome v1)
  → Objective: "Implement user authentication"
  → Planning → Implementation
  → OAuth2 need discovered → genome-change logged in backlog
  → Validation (partial)
  → Completion → Retrospective + genome update → Genome v2 → Archive

Generation #2 (Genome v2)
  → Objective: "OAuth2 integration + permission management"
  → Deferred tasks from previous generation + new goals
  → ...
```

## Project Structure (show as tree diagram)
```
my-project/
├── src/                          # Civilization (your code)
├── .reap/
│   ├── config.yml                # Project configuration
│   ├── genome/                   # Genetic information
│   │   ├── principles.md
│   │   ├── domain/
│   │   ├── conventions.md
│   │   └── constraints.md
│   ├── environment/              # External context
│   ├── life/                     # Current generation
│   │   ├── current.yml
│   │   └── backlog/
│   ├── lineage/                  # Completed generation archive
│   └── hooks/                    # SessionStart hook
│       ├── session-start.sh
│       └── reap-guide.md
└── .claude/
    ├── commands/                 # Claude Code slash commands
    └── hooks.json                # SessionStart hook registration
```

## Technical Notes
- Use VitePress with default theme, override CSS variables for the golden color scheme
- Dark mode only (no light mode toggle needed)
- Logo image will be uploaded — use it for nav logo, hero, and favicon
- Keep the design clean, minimal, and technological
- Use custom CSS components for diagrams (lifecycle flow, 3-layer model, four-axis grid) rather than plain markdown tables where visual impact matters
